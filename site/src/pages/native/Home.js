import React from 'react';
import config from './../../constants/config/home/native';
import Home from './../../components/Home';
import theme from './../../constants/theme/native';

export default () => <Home config={config} theme={theme} />;
