import React from 'react';
import config from '../../constants/config/learn/native';
import Learn from '../../components/Learn';
import theme from '../../constants/theme/native';

export default () => <Learn config={config} theme={theme} />;
